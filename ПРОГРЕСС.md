Держите полный обновлённый `ПРОГРЕСС.md` — с учётом разбиения на Blueprints и всех последних изменений.

```markdown
# Прогресс разработки электронного журнала

## Общая информация

- **Проект:** Электронный журнал преподавателя
- **Стек:** Python 3.14 + Flask 3.1 + SQLite + Bootstrap 5
- **Папка проекта:** `C:\Users\taulis\Desktop\Journal`
- **Виртуальное окружение:** `venv` (в `.gitignore`)
- **Репозиторий:** https://github.com/Taulisus/Journal
- **Логин:** `admin`
- **Пароль:** задаётся в `.env` через `ADMIN_INITIAL_PASSWORD` или генерируется
  автоматически при первом запуске и показывается один раз в консоли
- **Бекапы БД:** в папке `backups/` — НЕ УДАЛЯТЬ, не в git

---

## Что уже работает

### Основной функционал

- ✅ Авторизация с хешированием паролей (`werkzeug.security`)
- ✅ Пользователи, должности, персональные права
- ✅ Роли и права ролей (`position_permissions`)
- ✅ Группы, предметы, студенты (с импортом Excel/CSV)
- ✅ Пары Группа-Предмет (журналы) с динамическими таблицами `journal_<id>`
- ✅ Семестры (1, 2, 3) с часами по типам занятий
- ✅ Ведение журнала: посещаемость, оценки, темы
- ✅ Типы занятий: лекция, практика, С/Р, под запись, экзамен, дифф. зачёт
- ✅ Оценки за семестр
- ✅ Карточки студентов с графиками (matplotlib)
- ✅ Отчёты с графиками
- ✅ Статистика часов преподавателя (план/факт)
- ✅ Назначение преподавателя на журналы
- ✅ Массовый перевод студентов между группами (с бэкапом БД)
- ✅ Лог действий пользователей (`activity_log`) с фильтрами, экспортом CSV,
  скроллом таблицы и закреплённой шапкой
- ✅ Адаптивная вёрстка под iPhone 12 mini (375px)

### Расписание

- ✅ Blueprint `/schedule` — модели, справочники, страницы
- ✅ Табличный вид (день / неделя) — `/schedule?view=day|week`
- ✅ Фильтры: по группе, преподавателю, аудитории, дате
- ✅ Ручное создание/редактирование/удаление занятий
- ✅ Свободные аудитории — `/schedule/free-rooms`
- ✅ Справочники: преподаватели, аудитории, алиасы групп
- ✅ **Парсер PDF-расписания** (`parsers/pdf_schedule.py`)
  - 100% по паре/времени (585/585)
  - ~94% по предмету
  - ~99% по аудитории
  - fuzzy-матчинг преподавателей через `rapidfuzz` (fallback на `difflib`)
  - поиск по фамилии + инициалам (`Тетенькин Д.А.` → `Тетенькин Дмитрий Александрович`)
- ✅ **Импорт PDF через UI** — `/schedule/upload` с предпросмотром и чекбоксами
- ✅ **Право `edit_schedule`** — выдано ролям teacher / methodist / head_teacher / admin
- ✅ **Преподаватель может редактировать только свои занятия**
- ✅ **Виджет «Расписание на неделю»** на главной странице
  - 6 колонок (Пн–Сб)
  - фильтр по `users.teacher_id` (только свои занятия) либо все
  - навигация по неделям ← / → / Сегодня
  - подсветка сегодняшнего дня
  - клик по занятию → переход на `/schedule`
- ✅ **Связка «пользователь ↔ преподаватель» через UI** (`users.teacher_id`)
  - селект в форме создания/редактирования пользователя
  - бейдж с ФИО преподавателя в таблице пользователей

### Интервалы пар по дням недели

- ✅ `Config.TIME_INTERVALS_BY_DAY` (0=Пн .. 5=Сб)
  - Пн: 7 пар по 1 часу
  - Вт–Пт: 7 пар по 1.5 часа
  - Сб: 7 пар по 1 часу
- ✅ `Config.BREAK_INTERVALS` — перерывы (Пн, 12:10–12:55, 12:55–13:40)
- ✅ `Config.get_time_intervals(date_str)` — хелпер
- ✅ Динамические подсказки в `add_lesson.html` и `schedule/edit_lesson.html`

### Логи, бэкапы, экспорт

- ✅ `create_backup` в `utils.py` — с ротацией (30 бэкапов), проверкой целостности
- ✅ `export_journal_to_excel` в `utils.py` — экспорт журнала в `.xlsx`
- ✅ Роут `/journal/<gsid>/export` — скачать журнал в Excel
- ✅ Роут `/admin/backup` — ручной бэкап БД из UI
- ✅ Кнопка «Excel» на странице журнала
- ✅ Карточка «Резервная копия БД» на странице `/activity`
- ✅ Автоматические бэкапы перед массовым переводом и удалением журнала
- ✅ Скролл в блоке «Последние действия» на главной
- ✅ Скролл + закреплённая шапка в таблице действий

### Хелперы в `app.py` (context_processor)

- ✅ `format_time` — `0900-1030` → `09:00 – 10:30`
- ✅ `grade_color` — Bootstrap-класс по оценке
- ✅ `attendance_color` — Bootstrap-класс по посещению
- ✅ `get_time_intervals(date)` — интервалы для конкретной даты
- ✅ `all_time_intervals` — JSON для JS
- ✅ `day_names_short`, `day_names_full`

### Безопасность

- ✅ `SECRET_KEY` через `.env` (не в коде)
- ✅ Пароль admin не в коде и не печатается при каждом запуске
- ✅ `python-dotenv` для загрузки `.env`
- ✅ Декоратор `@permission_required(*codes)` с JSON-ответами для API
- ✅ `_is_api_request()` распознаёт `/schedule/api/` и `/api/`
- ✅ Проверки прав на мутирующих роутах
- ✅ Проверка владения журналом (`_user_owns_journal`, `_require_journal_access`)
- ✅ Проверка владения занятием (`_can_edit_lesson`)
- ✅ Валидация `entry_id` (принадлежит ли он `gsid`)
- ✅ Бэкап БД перед `DROP TABLE` и массовым переводом
- ✅ `.gitignore` — БД, бэкапы, `venv`, `.idea`, `__pycache__`, `.env`, PDF, Excel
- ✅ `viewport` без `user-scalable=no` (доступность)

### Архитектура

- ✅ **Разбиение `routes.py` на Blueprints** — `routes.py` с ~1300 строк сокращён до ~600
  - `blueprints/groups.py` — `groups_bp`, `/groups`
  - `blueprints/subjects.py` — `subjects_bp`, `/subjects`
  - `blueprints/students.py` — `students_bp` + `student_card_bp`, `/students`, `/student`
  - `blueprints/journals.py` — `journals_bp`, `/group-subjects`, `/journal`
  - `blueprints/users.py` — `users_bp` + `roles_bp`, `/users`, `/roles`
  - `blueprints/_helpers.py` — общие хелперы
  - В `main_bp` остались: dashboard, профиль, отчёты, часы, лог, бэкап
  - `schedule_bp` и `auth_bp` — уже были отдельными

### База данных

- **Версия схемы:** 6
- **Миграции:** `migrations/migration_v3.py`, `v4.py`, `v5.py`, `v6.py`
- **Идемпотентность:** `init_db()` создаёт все таблицы с `CREATE TABLE IF NOT EXISTS`

Таблицы:

- **Пользователи:** `users`, `user_profiles`, `positions`, `user_positions`,
  `permissions`, `user_permissions`, `position_permissions`
- **Учебный процесс:** `groups`, `subjects`, `group_subjects`,
  `group_subject_hours`, `students`, `student_semester_grades`
- **Журналы:** `journal_<gsid>` (по одной на пару группа-предмет)
- **Преподаватели:** `teachers`, `teacher_journals`, `curators`, `teacher_hours`
- **История и годы:** `academic_years`, `student_group_history`
- **Расписание:** `schedule_lessons`, `rooms`, `group_aliases`
- **Логи и кэш:** `activity_log`, `stats_cache`

Сиды в `init_db`:

- admin (пароль из `ADMIN_INITIAL_PASSWORD` или случайный)
- 5 должностей: admin, teacher, curator, head_teacher, methodist
- 12 прав (включая `edit_schedule`)
- текущий учебный год 2025/2026
- 7 базовых аудиторий
- права ролей по умолчанию

---

## Структура проекта

```
Journal/
├── .env                    (не в git)
├── .env.example            (в git, шаблон)
├── .gitignore
├── app.py                  фабрика приложения, регистрация blueprints, context_processor
├── config.py               Config, TIME_INTERVALS_BY_DAY, BREAK_INTERVALS, FLASK_HOST/PORT/DEBUG
├── auth.py                 Blueprint auth (login/logout)
├── routes.py               Blueprint main (dashboard, профиль, отчёты, часы, лог, бэкап)
├── routes_schedule.py      Blueprint schedule
├── decorators.py           login_required, permission_required, _is_api_request
├── models.py               get_db, init_db, все модели
├── models_schedule.py      AcademicYear, Teacher, Room, GroupAlias, ScheduleLesson
├── journal_manager.py      работа с journal_<gsid>
├── stats.py                статистика, кэш
├── utils.py                импорт студентов, графики, экспорт Excel, бэкапы, форматирование, цвета
├── activity.py             лог действий
├── create_user.py          CLI управления пользователями
├── import_teachers.py      импорт преподавателей из Excel
├── requirements.txt
├── ПРОГРЕСС.md             этот файл
├── backups/                (не в git)
├── blueprints/             Blueprint'ы по доменам
│   ├── __init__.py
│   ├── _helpers.py         has_emoji, _make_db_backup, _require_journal_access*
│   ├── groups.py           groups_bp
│   ├── subjects.py         subjects_bp
│   ├── students.py         students_bp + student_card_bp
│   ├── journals.py         journals_bp
│   └── users.py            users_bp + roles_bp
├── migrations/
│   ├── migration_v3.py
│   ├── migration_v4.py
│   ├── migration_v5.py
│   └── migration_v6.py     идемпотентная миграция + edit_schedule
├── parsers/
│   ├── __init__.py
│   └── pdf_schedule.py     парсер PDF-расписания
├── templates/
│   ├── base.html
│   ├── _breadcrumbs.html
│   ├── login.html
│   ├── dashboard.html      + виджет «Расписание на неделю»
│   ├── profile.html
│   ├── users.html          + селект «Преподаватель»
│   ├── roles.html
│   ├── activity.html       + скролл, бэкап, закреплённая шапка
│   ├── groups.html
│   ├── subjects.html
│   ├── students.html
│   ├── student_card.html
│   ├── group_subjects.html
│   ├── journal.html        + модалка editLessonTimeModal
│   ├── add_lesson.html     + динамические интервалы
│   ├── assign_journals.html
│   ├── teacher_hours.html
│   ├── reports.html
│   ├── report_detail.html
│   └── schedule/
│       ├── index.html
│       ├── edit_lesson.html    + динамические интервалы
│       ├── upload.html         + предпросмотр импорта PDF
│       ├── free_rooms.html
│       ├── teachers.html
│       ├── rooms.html
│       └── aliases.html
├── static/
│   ├── style.css
│   └── charts/             (не в git, генерируется)
├── uploads/
│   ├── schedules/
│   └── exports/
├── tests/                  (пусто — тесты отложены)
└── journal.db              (не в git)
```

---

## Что осталось на потом (технический долг)

### Безопасность

- ⏳ CSRF-защита (Flask-WTF)
- ⏳ Rate-limit на логине (Flask-Limiter)
- ⏳ Bootstrap с CDN → в `static/` для офлайн-режима

### Качество кода

- ⏳ N+1 в `group_subjects.html` (батчить `get_semesters`/`get_hours`)
- ⏳ Модалки студентов в цикле — раздувание HTML
- ⏳ Удалять старые графики из `static/charts/`
- ⏳ `routes.py` всё ещё 600 строк — можно вынести отчёты и лог в отдельные Blueprints

### Интеграция расписания с журналом

- ⏳ Подсказки из расписания в `add_lesson.html`
- ⏳ Пометка «✓ по расписанию» в `journal.html`
- ⏳ Кнопка «Взять из расписания» в конкретном журнале

### Инфраструктура

- ⏳ Закоммитить изменения в git
- ⏳ Отложенные тесты (pytest)

### Данные

- ⏳ Динамические таблицы `journal_<gsid>` — долг, но работает
- ⏳ Поле `groups.code`, `groups.academic_year_id` созданы, но не используются

---

## Команды

```bash
# Активация venv (Windows cmd)
cd C:\Users\taulis\Desktop\Journal
venv\Scripts\activate

# Установка зависимостей
python -m pip install -r requirements.txt

# Первый запуск
copy .env.example .env
notepad .env   # заполнить SECRET_KEY
python app.py

# Миграции
python migrations\migration_v6.py

# Импорт преподавателей
python import_teachers.py

# Управление пользователями
python create_user.py list
python create_user.py add ivanov 123456
python create_user.py reset admin НовыйПароль123

# Генерация SECRET_KEY
python -c "import secrets; print(secrets.token_hex(32))"

# Отладка парсера
python -m parsers.pdf_schedule "1-4_kursy_s_21_09-26_09_2026.pdf" 1
python -m parsers.pdf_schedule "1-4_kursy_s_21_09-26_09_2026.pdf" --summary
```

---

## Журнал изменений

### 25.09.2026 — v2.1

- **Разбиение `routes.py` на Blueprints:**
  - `blueprints/groups.py` — `groups_bp` (index, add, edit, delete)
  - `blueprints/subjects.py` — `subjects_bp` (index, add, edit, delete)
  - `blueprints/students.py` — `students_bp` + `student_card_bp`
    (index, add, edit, delete, import, mass-transfer, transfer-info, card)
  - `blueprints/journals.py` — `journals_bp` (группа-предмет, журнал,
    add-lesson, update-entry, update-lesson-type, delete-lesson,
    update-lesson-time, export, set-semester-grade, get-hours, edit-hours)
  - `blueprints/users.py` — `users_bp` + `roles_bp` (пользователи,
    назначения журналов, роли, права ролей)
  - `blueprints/_helpers.py` — `has_emoji`, `_make_db_backup`,
    `_user_owns_journal`, `_require_journal_access`, `_require_journal_access_json`
  - `routes.py` сокращён с ~1300 до ~600 строк
  - `app.py` регистрирует все Blueprints
  - Все `url_for('main.X')` в шаблонах заменены на `url_for('<blueprint>.X')`

### 24.09.2026 — v2.0

- **Расписание (Blueprint `/schedule`):**
  - Модели: `AcademicYear`, `Teacher`, `Room`, `GroupAlias`, `ScheduleLesson`
  - Роуты: день/неделя, CRUD занятий, справочники, свободные аудитории, API
  - Шаблоны: `index`, `edit_lesson`, `free_rooms`, `teachers`, `rooms`, `aliases`
- **Парсер PDF-расписания** (`parsers/pdf_schedule.py`):
  - 100% по паре/времени, 94% по предмету, 99% по аудитории
  - fuzzy-матчинг преподавателей (rapidfuzz + difflib)
  - поиск по фамилии + инициалам
  - подстановка времени из `Config` при пропуске в PDF
- **Импорт PDF через UI** (`/schedule/upload`):
  - предпросмотр с чекбоксами
  - автосоздание групп, предметов, преподавателей, аудиторий
  - `Teacher.find_or_create` с fuzzy-матчингом
- **Виджет «Расписание на неделю»** на главной
- **Право `edit_schedule`** — редактирование расписания преподавателями
- **Связка «пользователь ↔ преподаватель» через UI**
- **Скроллы:** блок «Последние действия» на главной, таблица на `/activity`
- **Бэкапы:** `create_backup` в `utils.py`, роут `/admin/backup`, кнопка в UI
- **Экспорт журнала в Excel:** `export_journal_to_excel`, роут `/journal/<id>/export`

### 23.09.2026 — v1.7

- Blueprint `schedule`, модели, справочники
- Безопасность: `SECRET_KEY` из `.env`, проверки владения журналом
- `migration_v6.py` — идемпотентная миграция

### 22.09.2026 — v1.6.x

- Часы преподавателя (план/факт), `stats.py`, `stats_cache`
- Журнал: семестровые оценки, мобильная версия с быстрыми отметками

### 22.09.2026 — v1.5

- `activity_log`, `stats_cache`
- Массовый перевод студентов с историей

### 21.09.2026 — v1.6

- Обновление `models.py`, `journal_manager.py`

### 18.09.2026 — v1.0–v1.5

- Базовый функционал, миграции v3–v5

---

## Полезные ссылки

- Репозиторий: https://github.com/Taulisus/Journal
- Bootstrap 5: https://getbootstrap.com/docs/5.3/
- Bootstrap Icons: https://icons.getbootstrap.com/
- Flask: https://flask.palletsprojects.com/
- pdfplumber: https://github.com/jsvine/pdfplumber
- rapidfuzz: https://github.com/maxbachmann/RapidFuzz
- openpyxl: https://openpyxl.readthedocs.io/
```

---

## Что изменилось в этом `ПРОГРЕСС.md`

1. **Версия v2.1** — от 25.09.2026.
2. **Новый раздел «Архитектура»** — описание разбиения `routes.py` на Blueprints.
3. **Обновлённая структура проекта** — добавлена папка `blueprints/` с шестью модулями.
4. **Обновлён журнал изменений** — запись v2.1 от 25.09.2026 про разбиение.
5. **Технический долг** — добавлено, что `routes.py` всё ещё 600 строк, можно вынести отчёты и лог.
6. **Команды** — без изменений.

---

## Что дальше

Скажите, что делаем:

1. **Закоммитить в git** — накопилось много изменений (парсер, расписание, бэкапы, Blueprints, связка user↔teacher).
2. **CSRF-защита** — Flask-WTF + глобальный `CSRFProtect`.
3. **Интеграция расписания с журналом** — подсказки из расписания в `add_lesson.html`.
4. **Что-то другое.**